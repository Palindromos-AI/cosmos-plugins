interface:
  display_name: "Dingding Fetch"
  short_description: "按北京单日或日期区间读取指定钉钉群消息并提取原文，可选筛选、可选不读图片"
  default_prompt: "使用 $dingding-fetch 读取指定钉钉群一个北京日期或日期区间的消息（可按要求筛选，默认全部；可选不读取图片），并保存到固定的 sources 工作区。"
